(robin (0 . 1) (small (0 . 1) concurrency (0 . 1) crude-io (0 . 1))
  (bind input-loop
        (fun (self)
          (recv entered
            (if (equal? entered (literal eof))
              #f
              (bind foo (call crude-output write (pair #t entered))
                (self self)))))
    (bind x (call crude-input subscribe ())
      (input-loop input-loop))))
